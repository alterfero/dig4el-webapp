{
  "name": "dig4el-lessons-webapp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lightweight DIG4EL lesson browser for Railway and local deployment.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch src/server.mjs",
    "start": "node src/server.mjs"
  },
  "dependencies": {
    "compression": "^1.8.0",
    "express": "^4.21.2",
    "pdfkit": "^0.17.1"
  }
}
